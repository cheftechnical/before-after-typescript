/**
 * This defines our a select menu item element looks like
 */
export default interface SelectMenuItem {
  label: string;  // This is what the user sees in the select component
  value: string;  // This is the value saved to the data
}
